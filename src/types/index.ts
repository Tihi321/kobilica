import type { LogoType, LogoColor, SlideButtonType } from "../constants";

export type TPostition = {
  x: number;
  y: number;
  z: number;
}

export type TMouse = {
  x: number;
  y: number;
}

export type TLogo = LogoType;

export type TLogoColor = LogoColor;

export type TSlideButton = SlideButtonType;