export enum Events {
  CameraForward = "onCameraPositionForward",
  CameraBackward = "onCameraPositionBackward",
  CameraPosition = "onCameraPosition",
};

export enum ColorOceanBlue {
  R = 0,
  G = 0.19,
  B = 0.4,
}

export enum ColorLightBlue {
  R = 0.1,
  G = 0.5,
  B = 1,
}

export enum Slides {
  About = 0,
  Services = 1,
  Contact = 2,
};

export enum LogoType {
  Full = "full",
  Mixed = "mixed",
  Stroke = "stroke"
}

export enum LogoColor {
  Blue = "#2A2A57",
  Yellow = "#F9CE3A",
  White = "#FFFFFF"
}

export enum SlideButtonType {
  Next = "next",
  Back = "back",
}